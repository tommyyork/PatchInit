Playing around with Cursor and a fork of the DaisyExamples repo. Potential directions:

- vcvrack harness for daisy patch.init() binaries
- phase vocoder style resynthesis module, with automated panel utilities and testing, in `patch_sm/Resynthesis`
- some ambisonic wav processing ideas (in `utils/process-soundfield`)

Code is messy right now, and constantly changing. Don't trust anything you don't audit yourself.